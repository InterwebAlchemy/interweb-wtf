export const metadata = {
  title: 'Interweb.WTF CLI API v0',
  description:
    'Interweb.WTF CLI API Documentation for v0 (beta) version of the CLI API.',
};

import Link from 'next/link';
import {
  Anchor,
  API_URL,
  BASE_URL,
  CURRENT_API_VERSION,
} from '@mantine/core';
import ApiBetaNotice from '@/app/_components/ApiBetaNotice';
import CodeBlock from '@/app/_components/DocsCodeblock';

## API

The Interweb.WTF CLI API is a simple API that allows you to interact with the most basic
features of the Interweb.WTF service.

This documentation covers the **{CURRENT_API_VERSION}** version of the Interweb.WTF CLI API.

<ApiBetaNotice />

## API URL

<CodeBlock lang="shell">{API_URL}</CodeBlock>

### Endpoints

- [Resolve WTF Links from Interweb.WTF with `/go{:shell}`](/docs/cli/v0/go)
- [Expand Shortlinks from other providers with `/is{:shell}`](/docs/cli/v0/is)
- [Clean tracking parameters from URLs with `/clean{:shell}`](/docs/cli/v0/clean)
